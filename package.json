{
  "name": "replay-cli",
  "private": true,
  "packageManager": "yarn@4.1.1",
  "workspaces": [
    "packages/*"
  ],
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "bootstrap": "lerna exec yarn run build",
    "test": "yarn run test:unit",
    "test:unit": "lerna exec yarn run test",
    "typecheck": "lerna exec yarn run typecheck",
    "lint": "prettier --check ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.7.1",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "node-fetch": "^3.3.1"
  },
  "resolutions": {
    "@types/node": "^20.11.27"
  }
}
