{
  "name": "@replayio/jest",
  "version": "0.1.0-alpha.1",
  "description": "Configuration utilities for using the Replay browsers with jest",
  "main": "index.js",
  "scripts": {
    "is-source": "node -e 'process.exit(require(\"fs\").existsSync(\"./install.js\") ? 1 : 0)'",
    "prepare": "npm run is-source || npm run build",
    "install": "npm run is-source || node ./install.js",
    "build": "rm -rf dist/ && tsc && cp package.json README.md dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "publishConfig": {
    "directory": "dist"
  },
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@jest/reporters": "^27.5.1",
    "@jest/test-result": "^27.5.1",
    "@types/node": "^17.0.21"
  },
  "dependencies": {
    "@replayio/replay": "^0.1.0-alpha.5"
  }
}
